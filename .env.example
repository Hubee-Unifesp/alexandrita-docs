# Credenciais usadas pelo middleware.js para proteger o site com HTTP Basic Auth.
# Configure os valores reais como variáveis de ambiente no projeto Vercel
# (Settings > Environment Variables), nunca commitando o .env real.
SITE_USER=
SITE_PASSWORD=
